// Deprecated compatibility file kept intentionally empty because this project
// uses standalone components and `bootstrapApplication` in `src/main.ts`.
// The original NgModule-based `AppModule` was removed during migration.

export const APP_MODULE_DEPRECATED = true;
